APP_NAME="SEMS"
APP_ENV=production
APP_KEY=base64:PRMgg8BebBCOjKNOW2jG1DZ93EAON+FoDUgq5fQ8Gd8=
APP_DEBUG=false
APP_URL=https://sem-production-091a.up.railway.app/

# Railway specific
RAILWAY_ENVIRONMENT=production
RAILWAY_PUBLIC_DOMAIN=https://sem-production-091a.up.railway.app/
RAILWAY_STATIC_URL=https://sem-production-091a.up.railway.app/

# Localization
APP_LOCALE=en
APP_FALLBACK_LOCALE=en

# Application Maintenance
APP_MAINTENANCE_DRIVER=file

# Server Configuration
PHP_CLI_SERVER_WORKERS=4
BCRYPT_ROUNDS=12

# Logging
LOG_CHANNEL=stack
LOG_LEVEL=debug
LOG_DEPRECATIONS_CHANNEL=null

# Database Configuration
DB_CONNECTION=mysql
DB_HOST=${MYSQLHOST}
DB_PORT=${MYSQLPORT}
DB_DATABASE=${MYSQLDATABASE}
DB_USERNAME=${MYSQLUSER}
DB_PASSWORD=${MYSQLPASSWORD}

# Session Configuration
SESSION_DRIVER=database
SESSION_LIFETIME=120
SESSION_ENCRYPT=true
SESSION_SECURE_COOKIE=true

# Cache & Queue
CACHE_DRIVER=redis
QUEUE_CONNECTION=redis
FILESYSTEM_DISK=public

# Vite Configuration
VITE_APP_NAME="${APP_NAME}"
VITE_APP_ENV="${APP_ENV}"
VITE_APP_URL="${APP_URL}"
VITE_DEV_SERVER_URL="${APP_URL}:5173"
VITE_DEV_SERVER_HOST="0.0.0.0"
VITE_PORT=5173

# Session Security
SESSION_SECURE_COOKIE=true
SESSION_SAME_SITE=lax

# Trusted Proxies
TRUST_PROXIES=*

# Redis (if using for cache/queue)
REDIS_CLIENT=phpredis
REDIS_HOST=127.0.0.1
REDIS_PASSWORD=null
REDIS_PORT=6379

# Email Configuration
MAIL_MAILER=smtp
MAIL_HOST=mailhog
MAIL_PORT=1025
MAIL_USERNAME=null
MAIL_PASSWORD=null
MAIL_ENCRYPTION=null
MAIL_FROM_ADDRESS="no-reply@example.com"
MAIL_FROM_NAME="${APP_NAME}"

AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_DEFAULT_REGION=us-east-1
AWS_BUCKET=
AWS_USE_PATH_STYLE_ENDPOINT=false

# Vite configuration
VITE_APP_NAME="${APP_NAME}"
VITE_APP_ENV=local
VITE_APP_URL="${APP_URL}"
VITE_PUSHER_APP_KEY="${PUSHER_APP_KEY}"
VITE_PUSHER_APP_CLUSTER="${PUSHER_APP_CLUSTER}"
